# AGENT 必知必会

# 归档命令 `/roll-up`

任何 AI 都可以参照此章节构建自己的命令。

## 归档规范 260816

### 触发词

`归档`、`帮我归档`、`执行归档`、`archive`

### 执行步骤

1. 回顾本次对话，识别有价值的内容（设计决策、踩坑原因、方案权衡、选型结论）
2. 判断每条内容归属哪个 `docs/note-*.md` 文件
3. 读取目标文件，确认内容未被记录过
4. 按文档结构规范写入，追加到文件顶部
5. 告知用户归档了哪些内容、写入了哪些文件

### 写入位置判断

| 文件 | 受众 | 内容 |
| --- | --- | --- |
| `README.md` | 使用者 | 是什么、怎么用、怎么启动——面向操作，不解释原因 |
| `docs/note-*.md` | 维护者 / 自己 | 设计决策、方案权衡、踩坑过程、背景原因——面向理解，不重复操作步骤 |

判断规则：内容是"怎么做" → 写 README；内容是"为什么这么做"或"遇到了什么" → 写 docs。

### 文档结构

- **一级标题**：按领域或问题域命名（即文件主题）
- **二级标题**：直接写结论，末尾附日期；能独立阅读，不依赖上下文；疑问句放正文，标题只陈述结论
- **排序**：时间倒序，新内容前置；同一主题的多个问题可合并到一个二级标题下

### 归档原则

- **去重优先**：先读目标文件，确认未记录过再写入
- **只归档有价值的内容**：设计决策、踩坑原因与解决过程、方案权衡、工具选型结论；不归档过渡性问答、试探性尝试、已被推翻的方案
- **格式服从规范**：按文档结构写入，时间倒序追加到文件顶部
- **不改原内容**：已有条目保持不动，新内容只追加
